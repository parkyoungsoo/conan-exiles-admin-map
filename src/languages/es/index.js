const items = require('./items')
const ui = require('./ui')

const spanishLanguage = {
  "NO_NAME": "Desconocido",
  "DATABASE_ERROR": "Ha habido un error al consultar la base de datos",
  items: items,
  ui: ui
}

module.exports = spanishLanguage
