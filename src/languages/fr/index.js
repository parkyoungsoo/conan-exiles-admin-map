const items = require('./items')
const ui = require('./ui')

const frenchLanguage = {
  "NO_NAME": "Inconnu",
  "DATABASE_ERROR": "There was an error while querying the database",
  items: items,
  ui: ui
}

module.exports = frenchLanguage
